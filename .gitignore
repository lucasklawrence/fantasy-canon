node_modules
dist
build
*.tsbuildinfo
.env
.env.local
.DS_Store
*.log
.turbo
coverage
*.stackdump
screenshots

# temp/scratch files (e.g. ESPN payload dumps)
tmp-*
*.tmp.json

# durable bot state, e.g. committed draft-order ceremonies — holds a pre-reveal seed (#176)
.data/

# ignore everything under .claude (local settings, caches, per-machine state)
# except shared slash commands and team settings (settings.local.json stays ignored)
.claude/*
!.claude/commands/
!.claude/settings.json
