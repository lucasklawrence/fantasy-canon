node_modules
dist
build
*.tsbuildinfo
.env
.env.local
.DS_Store
*.log
pnpm-lock.yaml
.turbo
coverage
*.stackdump

# ignore everything under .claude (local settings, caches, per-machine state)
# except shared slash commands and team settings (settings.local.json stays ignored)
.claude/*
!.claude/commands/
!.claude/settings.json
