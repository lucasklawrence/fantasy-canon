node_modules
dist
build
.env
.env.local
.DS_Store
*.log
pnpm-lock.yaml
.turbo
coverage

# ignore everything under .claude (local settings, caches, per-machine state)
# except the shared, tracked slash commands
.claude/*
!.claude/commands/
